export interface VideoClip {
    video: number,
    start_minutes: number,
    start_seconds: number,
    end_minutes: number,
    end_seconds: number,
    subtitle_text: string,
    pictureUrl: string
}
